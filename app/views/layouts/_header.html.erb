<nav class='navbar navbar-default affix' style='width: 100%; z-index: 100' role='navigation'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <a class="navbar-brand" href=''>Home</a>
    </div>

    <div class='collapse navbar-collapse'>
      <ul class='nav navbar-nav'>

        <li>
          <span class="glyphicon glyphicon-search search-icon"></span>
          <form class='search-form'>
            <input type='text' id='search-bar' value="King's Landing, Westeros">
            <button type='submit' id='search-button'></button>
          </form>
        </li>
      </ul>
      <ul class='nav navbar-nav navbar-right'>
        <li class='dropdown'>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= image_tag current_user.avatar.url(:xs), class: 'img-circle' %>
              <%= current_user.first_name %>
              <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#/profile/<%= current_user.id %>">Profile</a></li>
            <li>
                <a href="#/requests/<%= current_user.id %>">
                    Requests
                    <% unless current_user.bookings.where('status = ?', 'PENDING').empty? %>
                      <div class='notification'>
                        <%= current_user.bookings.where('status = ?', 'PENDING').length %>
                      </div>
                    <% end %>
                </a>
            </li>
            <li><a href="#/listings/<%= current_user.id %>">Your Listings</a></li>
            <li><a href="#/reservations/<%= current_user.id %>">Your Reservations</a></li>

            <li class="divider"></li>
            <li><%= link_to "Log Out", session_url, method: :delete%></li>
          </ul>
        </li>
        <li>
          <button href='#/spaces/new' class='space-new'>
            List Your Space
          </button>
        </li>
      </ul>
    </div>
  </div>
</nav>
